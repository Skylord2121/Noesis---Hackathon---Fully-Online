{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "whisper": "python whisper_server.py",
    "whisper:install": "pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118 && pip install openai-whisper flask flask-cors ffmpeg-python",
    "whisper:test": "python -c \"import whisper; import torch; print('Whisper:', whisper.__version__); print('GPU:', torch.cuda.is_available())\"",
    "whisper:health": "curl http://localhost:5000/health"
  },
  "dependencies": {
    "hono": "^4.9.12"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}